= form_with url: search_posts_path, method: :get, local: true do |f|
  .search_form
    = f.text_field :keyword, placeholder: '種(亜種)名を入力してください...', class: 'search_field'
    .abc
      カラスバト
    /= f.submit '検索する'